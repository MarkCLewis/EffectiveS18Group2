package roads;

import virtualworld.terrain.Pair;

public class Road {

	//creates Road object with center point and length
	public Road(Pair<Double, Double> cen, double leng) {
		center = cen;
		length = leng;
	}
	
	//need function to determine length. Goes from city to city on an optimal path
	
}
