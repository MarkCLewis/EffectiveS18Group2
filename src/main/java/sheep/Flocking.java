package sheep;

public class Flocking {
	//Alignment-- causes a particular agent to line up with agents closeby
	
	//Function that takes an agent and returns a velocity vector
	//Sheep are the agents in this case, void should be location
/*	public Point computeAllignment(Sheep sheep){
		Point v;
		int herdCount = 0;
		
		//iteration through all sheep to find ones close enough to be neighbors
		/* for each
		 * 
		 
		
		return v;
	}*/
	
	//Cohesion
	//Separation
}
